package main

func main() {

	/* Day One - Part One: Get the results to calibrate the trebuchet
	from the codes strings get the numbers and from the frst and last digit
	create a number. Then add the all up
	*/

	// dayOneResults, errDayOne := dayOneFunc()
	// if errDayOne != nil {
	// 	log.Panic(errDayOne)
	// }

	// log.Printf("Day One - Part One: Get the sum of all codes to calibrate the Trebuchet?!: %v", dayOneResults)

	/* Day One - Part Two: do the same but with the letters that create numbers*/

	dayOnePartTwoFunc()
	// if errDayOnePartTwo != nil {
	// 	log.Panic(errDayOnePartTwo)
	// }

	// log.Printf("Day One - Part Two: Get the sum of all codes but from the letters. Calibrate BETTER: %v", dayOnePartTwoResults)
}
